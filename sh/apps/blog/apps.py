from django.apps import AppConfig

class BlogConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.blog' #3.cAfter we add our apps in setting.py we add  the word  apps. here 4

